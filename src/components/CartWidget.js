import React from "react";

const CartWidget = () => {
    return(
        <button className="btn btn-outline-success ms-auto">
            Carrito
        </button>
    );
};

export default CartWidget;